---
phase: 02-core-quiz-flow
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - src/components/summary/score-reveal.tsx
  - src/components/summary/quiz-stats.tsx
  - src/components/summary/wrong-answer-carousel.tsx
  - src/components/summary/category-radar.tsx
  - src/components/summary/smart-actions.tsx
  - src/app/(app)/practice/summary/page.tsx
  - src/app/(app)/simulated-test/summary/page.tsx
  - src/components/ui/chart.tsx
  - src/lib/utils/quiz-summary.ts
autonomous: false
requirements:
  - ANAL-01
  - ANAL-06

must_haves:
  truths:
    - "After completing a practice quiz, user sees animated score counting up from 0 to final percentage"
    - "Summary shows streak stats, best run, and time taken"
    - "Wrong answers are displayed as a swipeable card carousel with question, user's answer, correct answer, and explanation"
    - "Category performance radar chart shows all 8 categories as axes"
    - "Smart actions at bottom: Try Again, Drill Weakest Category, Back to Dashboard"
    - "After sim test: DMV letter result transitions to detailed summary"
    - "Skip button on score animation for repeat users"
    - "Summary data persists and is queryable for future analytics"
  artifacts:
    - path: "src/components/summary/score-reveal.tsx"
      provides: "Animated score count-up display"
      min_lines: 30
    - path: "src/components/summary/wrong-answer-carousel.tsx"
      provides: "Swipeable card carousel for wrong answer review"
      min_lines: 50
    - path: "src/components/summary/category-radar.tsx"
      provides: "Radar chart with 8 category axes"
      min_lines: 40
    - path: "src/components/summary/smart-actions.tsx"
      provides: "Try Again / Drill Weakest / Dashboard buttons"
      min_lines: 25
    - path: "src/app/(app)/practice/summary/page.tsx"
      provides: "Practice quiz summary page"
      min_lines: 20
  key_links:
    - from: "src/app/(app)/simulated-test/summary/page.tsx"
      to: "src/components/summary/dmv-result-letter.tsx"
      via: "renders DmvResultLetter at top of sim summary with pass/fail data"
      pattern: "DmvResultLetter"
    - from: "src/app/(app)/practice/summary/page.tsx"
      to: "src/lib/utils/quiz-summary.ts"
      via: "computes summary data from quiz results"
      pattern: "computeQuizSummary"
    - from: "src/components/summary/category-radar.tsx"
      to: "recharts"
      via: "RadarChart component"
      pattern: "RadarChart.*PolarGrid.*Radar"
    - from: "src/components/summary/wrong-answer-carousel.tsx"
      to: "motion/react"
      via: "drag gestures for swipe"
      pattern: "drag.*onDragEnd"
    - from: "src/components/summary/smart-actions.tsx"
      to: "src/lib/utils/quiz-summary.ts"
      via: "identifies weakest category for drill recommendation"
      pattern: "weakestCategory|categoryBreakdown"
---

<objective>
Build the post-quiz summary screen with animated score reveal, wrong answer carousel, category radar chart, and smart actions for both practice and sim modes.

Purpose: The summary screen is where learning happens after the quiz. It shows the user exactly what they got wrong, why, and which categories to focus on. Per user decision, this screen should feel like a "gaming stats screen" with cyberpunk energy.

Output: Summary page routes for practice and sim, animated score components, swipeable wrong answer cards, radar chart, smart action buttons.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-quiz-flow/02-CONTEXT.md
@.planning/phases/02-core-quiz-flow/02-RESEARCH.md
@.planning/phases/02-core-quiz-flow/02-01-SUMMARY.md
@.planning/phases/02-core-quiz-flow/02-02-SUMMARY.md
@.planning/phases/02-core-quiz-flow/02-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior plans -->

From src/lib/types/quiz.ts (Plan 01):
```typescript
interface QuizAnswer {
  questionId: string
  selectedAnswer: string
  correctAnswer: string
  isCorrect: boolean
  timeTakenMs: number
}

interface PreparedQuestion {
  id: string
  questionText: string
  correctAnswer: string
  shuffledOptions: string[]
  explanation: string
  handbookReference: string
  categorySlug: string
  categoryName: string
}

interface QuizSessionResult {
  sessionId: string
  score: number
  total: number
  passed: boolean | null
  answers: QuizAnswer[]
  mode: QuizMode
  bestStreak: number
  totalTimeMs: number
  startedAt: string
  completedAt: string | null
  isComplete: boolean
}
```

From src/lib/utils/quiz-summary.ts (Plan 02):
```typescript
interface QuizSummaryData {
  score: number
  total: number
  percentage: number
  wrongAnswers: QuizAnswer[]
  categoryBreakdown: { categoryName: string, correct: number, total: number, percentage: number }[]
  bestStreak: number
}
export function computeQuizSummary(answers: QuizAnswer[], questions: PreparedQuestion[]): QuizSummaryData
```

From src/components/summary/dmv-result-letter.tsx (Plan 03):
```typescript
// DMV letter-style pass/fail result component
// Props: { score: number, total: number, passed: boolean }
// Rendered by sim summary page at top of layout
```

From src/lib/constants/categories.ts (Phase 1):
```typescript
interface Category {
  slug: CategorySlug
  name: string
  description: string
  icon: string
  weight: number
  sortOrder: number
}
export const CATEGORIES: Category[]
```

From src/lib/constants/quiz-config.ts (Plan 01):
```typescript
export const DMV_CONFIG = { totalQuestions: 46, passingScore: 38, passingPercentage: 0.83 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score reveal, quiz stats, and wrong answer carousel</name>
  <files>src/components/summary/score-reveal.tsx, src/components/summary/quiz-stats.tsx, src/components/summary/wrong-answer-carousel.tsx</files>
  <action>
    **src/components/summary/score-reveal.tsx** -- Animated score count-up:
    - Per Research code example (Animated Score Count-Up with Motion)
    - Uses Motion useMotionValue + useTransform + animate
    - Counts up from 0 to final score over 2 seconds with easeOut easing
    - Display: large score "{score}/{total}" with neon-cyan font-display
    - Below: percentage in neon-pink
    - Skip button: "Skip" text button that immediately sets count to final value (setIsComplete(true), count.set(score))
    - Per user decision: "Animated score reveal that counts up from 0 to final percentage, with a skip button for repeat runs"
    - On completion of animation, fire a celebration confetti burst (if score >= 70% use medium tier, if >= 90% use fireworks)

    **src/components/summary/quiz-stats.tsx** -- Session statistics:
    - Card showing: best streak (with streak-colored badge), total time taken (formatted as "Xm Ys"), questions answered correctly rate
    - Per user decision: "Streak stats, best run in session, time taken included"
    - Use Card component from shadcn/ui
    - Three stat items in a horizontal row: Best Streak (fire icon + number), Time (clock icon + formatted duration), Accuracy (target icon + percentage)
    - Cyberpunk-styled with neon accents on stat values

    **src/components/summary/wrong-answer-carousel.tsx** -- Swipeable card carousel:
    - Per Research Pattern 6 (Swipeable Card Carousel with Motion Drag)
    - Each card shows:
      - Question number + category badge
      - Question text
      - "Your answer: {selectedAnswer}" in red text
      - "Correct answer: {correctAnswer}" in green text
      - Explanation text
      - "CA DMV Handbook: {handbookReference}" in muted text
    - Swipe left/right to navigate between wrong answers
    - Motion drag="x" with dragConstraints, dragElastic, onDragEnd
    - SWIPE_THRESHOLD=100px, SWIPE_VELOCITY=500px/s
    - AnimatePresence with slide left/right transitions
    - Dot indicators below showing current card position (active dot wider + neon-pink)
    - Also support arrow buttons for accessibility (left/right chevrons)
    - Card styling: cyberpunk flashcard feel -- dark card with neon border, gradient accents
    - Per user decision: "Wrong answer review as swipeable card carousel -- each card shows question, her answer, correct answer, and explanation. Cyberpunk-styled flashcard feel."
    - If no wrong answers (perfect score): show a congratulations message instead of carousel
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Score reveal animates from 0 to final score with skip button. Quiz stats show streak, time, accuracy. Wrong answer carousel is swipeable with full question details, explanation, and handbook reference on each card.</done>
</task>

<task type="auto">
  <name>Task 2: Radar chart, smart actions, and summary pages</name>
  <files>src/components/summary/category-radar.tsx, src/components/summary/smart-actions.tsx, src/components/ui/chart.tsx, src/app/(app)/practice/summary/page.tsx, src/app/(app)/simulated-test/summary/page.tsx</files>
  <action>
    **First, add shadcn chart component** if not already present:
    ```bash
    npx shadcn@latest add chart
    ```
    This installs the ChartContainer, ChartConfig, ChartTooltip components that wrap Recharts.

    **src/components/summary/category-radar.tsx** -- Radar chart:
    - Per Research Pattern 7 (Radar Chart with shadcn/ui + Recharts)
    - Uses RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar from recharts
    - Wrapped in ChartContainer from shadcn/ui for automatic dark mode theming
    - Data: 8 categories as axes, score percentage (0-100) for each category
    - Use SHORT category names for mobile (Research pitfall #6): "Signs", "Right-of-Way", "Traffic", "Speed", "DUI", "Safe Driving", "Parking", "Sharing"
    - Add a `shortName` property when mapping categories for the chart
    - Chart config: score color = var(--neon-cyan), fill opacity 0.25, stroke width 2
    - Responsive: 300px height on desktop, 250px on mobile
    - Per user decision: "Category performance shown as a radar/spider chart with all 8 categories as axes. Visual shape of strengths/weaknesses. Gaming stats screen energy."
    - If user got 0 questions in a category (no data), show 0 for that axis (not missing)

    **src/components/summary/smart-actions.tsx** -- Action buttons:
    - Three buttons in a horizontal row (stacked on mobile):
      1. "Try Again" -- restarts the same mode (practice or sim). Links to /practice or /simulated-test.
      2. "Drill {Weakest Category}" -- identifies the category with lowest percentage score from categoryBreakdown, shows that category name. Links to /practice?category={slug} (category drill is Phase 4, but the link is ready). For now, this can link to /practice since category drill doesn't exist yet. Include a data-category attribute for future wiring.
      3. "Back to Dashboard" -- links to /dashboard
    - The "smart-recommended" action (Drill Weakest) gets a special visual treatment: neon-pink border/glow, slightly larger, "Recommended" badge
    - Per user decision: "Three paths, one is smart-recommended."
    - Use Button component from shadcn/ui with different variants

    **src/app/(app)/practice/summary/page.tsx** -- Practice summary page:
    - Route: /practice/summary
    - Receives quiz result data via URL searchParams (sessionId) or via Zustand store (ephemeral -- read before reset)
    - Strategy: The quiz store holds the results after completeQuiz(). The summary page reads from store on mount. If store is empty (page refresh), fetch from Supabase using sessionId from URL params.
    - Layout: scrollable full-screen page with dark background
    - Sections in order:
      1. ScoreReveal (animated count-up)
      2. QuizStats (streak, time, accuracy)
      3. CategoryRadar (radar chart)
      4. WrongAnswerCarousel (if any wrong answers)
      5. SmartActions (Try Again / Drill / Dashboard)
    - After quiz-shell completes, navigate to /practice/summary?session={sessionId}

    **src/app/(app)/simulated-test/summary/page.tsx** -- Sim summary page:
    - Route: /simulated-test/summary
    - Same layout as practice summary BUT with DmvResultLetter at the top (from Plan 03)
    - Imports and renders DmvResultLetter from src/components/summary/dmv-result-letter.tsx (created in Plan 03)
    - Order:
      1. DmvResultLetter (pass/fail reveal)
      2. "View Full Results" button or auto-transition after 3 seconds
      3. ScoreReveal (counts up)
      4. QuizStats
      5. CategoryRadar
      6. WrongAnswerCarousel
      7. SmartActions
    - Per user decision: "DMV letter-style pass/fail reveal... Official feel, then transitions to the detailed summary"

    **Wire up quiz completion -> summary navigation:**
    - Update the quiz-shell completeQuiz flow (or use-quiz hook) to navigate to the appropriate summary page after saveQuizResults:
      - Practice: router.push(`/practice/summary?session=${sessionId}`)
      - Sim: router.push(`/simulated-test/summary?session=${sessionId}`)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Radar chart renders 8 categories with score percentages. Smart actions show Try Again, Drill Weakest (smart-recommended), and Dashboard. Practice summary page composes all components. Sim summary shows DMV letter first, then full breakdown. Quiz completion navigates to correct summary page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 2 end-to-end verification</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 2 quiz flow across practice and simulated test modes.

    **What was built:** Complete Phase 2 quiz flow: practice mode with 15 questions, immediate feedback, celebrations, sound FX. Simulated DMV test with 46 questions, no mid-test feedback, flag-for-review, DMV letter-style result. Post-quiz summaries with animated score, wrong answer carousel, radar chart, and smart actions. All results persist to Supabase.
  </action>
  <verify>
    1. Start dev server: `npm run dev`
    2. Go to http://localhost:3000/dashboard
    3. Verify "Quick Practice" and "Simulated Test" cards are active (not disabled)

    **Practice Flow:**
    4. Click "Quick Practice" -- should start a 15-question quiz
    5. Answer a question correctly -- verify: green highlight, confetti particles, auto-advance after ~1.5 seconds
    6. Answer a question incorrectly -- verify: red highlight on wrong, green on correct, gentle shake animation, explanation slides in below with handbook reference, must tap "Next" to continue
    7. Build a streak of 5+ correct -- verify celebrations intensify
    8. Check the XP-bar progress fills with correct answers and glows on streaks
    9. Tap X to quit mid-quiz -- verify confirmation dialog appears, tap "Quit" and verify it saves partial results
    10. Start another practice quiz and complete all 15 questions
    11. Verify summary page: animated score count-up, stats, radar chart with 8 categories, wrong answer cards (swipeable), smart actions (Try Again / Drill Weakest / Dashboard)
    12. Swipe through wrong answer cards on mobile or drag on desktop

    **Simulated Test Flow:**
    13. Go to /simulated-test -- should start a 46-question quiz
    14. Verify SIM badge visible, no green/red feedback on answers
    15. Flag a few questions using the flag button
    16. Answer all 46 questions
    17. Verify flagged review screen appears with flagged question numbers
    18. Tap a flagged question number to revisit and change answer
    19. Submit the test
    20. Verify DMV letter-style pass/fail result appears
    21. Verify full summary with radar chart and wrong answer cards

    **Mobile:**
    22. Test the entire flow on a mobile viewport (responsive layout, touch targets, swipe)
  </verify>
  <done>User approves: both practice and sim quiz flows work end-to-end with correct feedback, celebrations, summaries, and persistence. Mobile experience is smooth.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx tsc --noEmit` -- no TypeScript errors
- Practice flow: start -> answer -> feedback -> summary is complete
- Sim flow: start -> answer (no feedback) -> flag review -> DMV letter -> summary is complete
- Radar chart renders 8 categories readable on both desktop and mobile
- Wrong answer carousel is swipeable with smooth animations
- Smart actions link correctly
- Results saved to Supabase (check DB directly or via Supabase dashboard)
</verification>

<success_criteria>
- Post-quiz summary shows score, wrong answers, category breakdown (ANAL-01)
- All quiz history persists in Supabase quiz_sessions + quiz_answers tables (ANAL-06)
- Animated score counts up from 0 with skip button
- Wrong answer carousel is swipeable with question details + explanations
- Radar chart shows all 8 categories with per-category score
- Smart actions: Try Again, Drill Weakest (recommended), Dashboard
- Sim summary includes DMV letter-style result before full summary
- Phase 2 human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-quiz-flow/02-04-SUMMARY.md`
</output>
