---
phase: 03-analytics-category-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(app)/dashboard/page.tsx
  - src/components/dashboard/readiness-gauge.tsx
  - src/components/dashboard/category-card.tsx
  - src/components/dashboard/category-grid.tsx
  - src/components/dashboard/category-sparkline.tsx
  - src/components/dashboard/quiz-history-list.tsx
  - src/components/dashboard/empty-state.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/bottom-tabs.tsx
autonomous: true
requirements: [ANAL-02, ANAL-03, ANAL-04, ANAL-05, DRILL-02]

must_haves:
  truths:
    - "User sees a persistent dashboard with readiness score hero at top, category cards below, quiz history at bottom"
    - "Dashboard shows category mastery percentages color-coded red/yellow/green"
    - "Dashboard shows mini sparkline trends per category (last 5-10 sessions)"
    - "Dashboard highlights weakest categories with visual emphasis"
    - "Readiness gauge is an encouraging circular ring that shifts color with score"
    - "Empty state shows friendly message for users with no quiz data"
    - "Analytics nav link removed from sidebar and bottom tabs (dashboard IS analytics)"
  artifacts:
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Server component dashboard fetching analytics data"
      min_lines: 30
    - path: "src/components/dashboard/readiness-gauge.tsx"
      provides: "Circular RadialBarChart gauge for readiness score"
      contains: "RadialBarChart"
    - path: "src/components/dashboard/category-card.tsx"
      provides: "Single category card with mastery %, sparkline, question count"
      contains: "CategorySparkline"
    - path: "src/components/dashboard/category-grid.tsx"
      provides: "Responsive grid of category cards"
      contains: "CategoryCard"
    - path: "src/components/dashboard/category-sparkline.tsx"
      provides: "Mini Recharts LineChart sparkline with no axes"
      contains: "LineChart"
    - path: "src/components/dashboard/quiz-history-list.tsx"
      provides: "Recent quiz history section"
    - path: "src/components/dashboard/empty-state.tsx"
      provides: "Friendly empty state for 0 quiz sessions"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/server/actions/analytics.ts"
      via: "fetchUserAnalytics server action call"
      pattern: "fetchUserAnalytics"
    - from: "src/components/dashboard/category-card.tsx"
      to: "/practice?category="
      via: "Link href with category slug"
      pattern: "href.*practice.*category"
    - from: "src/components/dashboard/readiness-gauge.tsx"
      to: "recharts RadialBarChart"
      via: "Recharts v2 RadialBarChart component"
      pattern: "RadialBarChart"
---

<objective>
Replace the placeholder dashboard with a data-driven analytics hub showing readiness score gauge, category mastery card grid with sparklines, and recent quiz history. Remove the separate Analytics nav entry per D-02.

Purpose: This is the primary user-facing analytics experience -- users see exactly where they are strong/weak and get motivated by the readiness gauge. The dashboard becomes the command center for targeted practice.

Output: Complete dashboard UI with all visualization components, consuming data from Plan 01's server actions.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-category-intelligence/03-CONTEXT.md
@.planning/phases/03-analytics-category-intelligence/03-RESEARCH.md
@.planning/phases/03-analytics-category-intelligence/03-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/lib/types/analytics.ts (created by Plan 01):
```typescript
export interface CategoryMastery {
  slug: string
  name: string
  weight: number
  correct: number
  total: number
  masteryPct: number
  recentMastery: number | null
  trendData: { sessionId: string; pct: number; date: string }[]
  questionCount: number
}

export interface SessionTrend {
  sessionId: string
  mode: string
  score: number
  total: number
  startedAt: string
  percentage: number
}

export interface UserAnalytics {
  categories: CategoryMastery[]
  sessionTrends: SessionTrend[]
  missedQuestionIds: string[]
  totalSessions: number
  readinessScore: number
  readinessMessage: string
}
```

From src/lib/utils/mastery.ts (created by Plan 01):
```typescript
export function getMasteryColor(percentage: number): string
export function getMasteryLevel(percentage: number): "strong" | "moderate" | "weak"
export function getWeakestCategories(categories: CategoryMastery[], limit: number): CategoryMastery[]
```

From src/server/actions/analytics.ts (created by Plan 01):
```typescript
export async function fetchUserAnalytics(): Promise<UserAnalytics | null>
```

From existing codebase:
```typescript
// src/components/ui/chart.tsx - shadcn chart wrapper
export function ChartContainer({ config, children, className })
// src/components/ui/card.tsx
export function Card, CardHeader, CardTitle, CardContent, CardDescription
// src/components/ui/badge.tsx
export function Badge
```

From src/lib/constants/categories.ts:
```typescript
export const CATEGORIES: Category[] // 8 categories with slug, name, icon, weight
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard visualization components (readiness gauge, sparkline, category card, grid, history, empty state)</name>
  <files>src/components/dashboard/readiness-gauge.tsx, src/components/dashboard/category-sparkline.tsx, src/components/dashboard/category-card.tsx, src/components/dashboard/category-grid.tsx, src/components/dashboard/quiz-history-list.tsx, src/components/dashboard/empty-state.tsx</files>
  <read_first>src/lib/types/analytics.ts, src/lib/utils/mastery.ts, src/lib/utils/readiness.ts, src/components/ui/chart.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/summary/category-radar.tsx, src/components/summary/smart-actions.tsx, src/lib/constants/categories.ts</read_first>
  <action>
    All components are "use client" since they use Recharts or interactive elements.

    Create src/components/dashboard/readiness-gauge.tsx:
    - Props: { score: number; message: string }
    - Use Recharts v2 RadialBarChart (NOT v3 API) inside ChartContainer from @/components/ui/chart
    - Semi-circular ring (startAngle={180}, endAngle={0}) with innerRadius="80%" outerRadius="100%"
    - PolarAngleAxis type="number" domain={[0, 100]} tick={false}
    - RadialBar with background fill "var(--surface-container-low)" and cornerRadius={10}
    - Color from getMasteryColor(score): >= 83 neon-cyan, >= 60 neon-purple, < 60 neon-pink
    - Large score text centered: font-display text-5xl (or text-6xl) showing "{score}%"
    - Message text below score: font-ui text-sm text-muted-foreground
    - When score is 0 or null, show "--%" with message "Complete quizzes to see your readiness score"
    - Per D-09: encouraging circular gauge that shifts color as score improves
    - Per D-10: positive framing always
    - Wrap the whole thing in a section with visual hierarchy (this is the HERO per D-03)

    Create src/components/dashboard/category-sparkline.tsx:
    - Props: { data: { value: number }[]; color?: string }
    - Recharts v2 LineChart inside ResponsiveContainer (width="100%" height={32})
    - Line with type="monotone", dataKey="value", stroke={color || "var(--neon-cyan)"}, strokeWidth={1.5}, dot={false}
    - isAnimationActive={false} (per Recharts v2 API)
    - No axes, no grid, no tooltip -- pure sparkline
    - Return null if data.length < 2 (per Pitfall 1)

    Create src/components/dashboard/category-card.tsx:
    - Props: { category: CategoryMastery }
    - Uses Card from @/components/ui/card
    - Shows: category name (font-display), mastery percentage (colored via getMasteryColor), sparkline (from trendData mapped to {value: pct}), question count (e.g., "42 questions answered")
    - Color-coded mastery per D-08: use getMasteryColor for the percentage text and a subtle left border
    - If masteryPct is 0 and total is 0, show "--%" with muted styling
    - Weakest categories get a "Needs Work" badge (use getMasteryLevel === "weak")
    - Card is wrapped in a Link to /practice?category={category.slug} per D-07 and D-12
    - Hover state with cyberpunk neon glow matching the project's existing card hover pattern from dashboard
    - Per D-06: card shows category name, mastery %, mini sparkline, question count

    Create src/components/dashboard/category-grid.tsx:
    - Props: { categories: CategoryMastery[] }
    - Responsive grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4, gap-4
    - Maps categories to CategoryCard components
    - Sorts to show weakest categories first (ascending masteryPct) per D-04 (visual hierarchy ensuring weakest pop)

    Create src/components/dashboard/quiz-history-list.tsx:
    - Props: { sessions: SessionTrend[] }
    - Shows last 5-10 recent quiz sessions in a compact list
    - Each row: mode badge (Practice/Sim), score (e.g., "14/15"), percentage, date (relative: "2 hours ago", "Yesterday")
    - Use simple date formatting (no library needed -- Intl.RelativeTimeFormat or manual logic)
    - Cyberpunk styling consistent with existing cards

    Create src/components/dashboard/empty-state.tsx:
    - Props: { variant: "unauthenticated" | "no-data" }
    - "no-data" variant: encouraging message "No quizzes yet -- start practicing to track your progress!" with links to /practice and /simulated-test
    - "unauthenticated" variant: "Sign in to track your progress" with link to /login
    - Use project's gradient text and neon styling for visual appeal
    - Per Pitfall 1: this is the fallback when totalSessions === 0
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RadialBarChart" src/components/dashboard/readiness-gauge.tsx
    - grep -q "ResponsiveContainer" src/components/dashboard/category-sparkline.tsx
    - grep -q "LineChart" src/components/dashboard/category-sparkline.tsx
    - grep -q "getMasteryColor" src/components/dashboard/category-card.tsx
    - grep -q "CategoryCard" src/components/dashboard/category-grid.tsx
    - grep -q "practice.*category" src/components/dashboard/category-card.tsx
    - grep -rq "empty-state" src/components/dashboard/
    - npx tsc --noEmit exits cleanly
  </acceptance_criteria>
  <done>All 6 dashboard visualization components created with correct Recharts v2 API usage, cyberpunk styling, and proper type consumption from Plan 01's analytics types. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard page and clean up navigation</name>
  <files>src/app/(app)/dashboard/page.tsx, src/components/layout/sidebar.tsx, src/components/layout/bottom-tabs.tsx</files>
  <read_first>src/app/(app)/dashboard/page.tsx, src/components/layout/sidebar.tsx, src/components/layout/bottom-tabs.tsx, src/server/actions/analytics.ts, src/lib/types/analytics.ts, src/components/dashboard/readiness-gauge.tsx, src/components/dashboard/category-grid.tsx, src/components/dashboard/quiz-history-list.tsx, src/components/dashboard/empty-state.tsx</read_first>
  <action>
    Rewrite src/app/(app)/dashboard/page.tsx as a server component:
    - Import fetchUserAnalytics from @/server/actions/analytics
    - Import ReadinessGauge, CategoryGrid, QuizHistoryList, EmptyState from @/components/dashboard/
    - Import createClient from @/lib/supabase/server
    - Fetch user via supabase.auth.getUser()
    - If no user, render EmptyState variant="unauthenticated"
    - Call fetchUserAnalytics() to get analytics data
    - If analytics is null or totalSessions === 0, render EmptyState variant="no-data" (keep the welcome greeting)
    - Otherwise render the full dashboard with visual hierarchy per D-03:
      1. Welcome greeting at top (keep the existing gradient "Welcome back, {displayName}" pattern)
      2. ReadinessGauge hero section
      3. CategoryGrid with all categories
      4. QuizHistoryList with recent sessions
    - Use the same max-w-5xl space-y-8 p-6 lg:p-8 layout container as existing
    - Quick action buttons for Practice and Sim Test below welcome greeting (keep existing functionality)

    Update src/components/layout/sidebar.tsx:
    - Per D-02: Remove the "Analytics" nav item (href="/analytics") from NAV_ITEMS array
    - Keep: Dashboard, Practice, Simulated Test, Flashcards (4 items)
    - Per Pitfall 6: this prevents 404 errors when clicking Analytics

    Update src/components/layout/bottom-tabs.tsx:
    - Per D-02: Remove the "Stats" tab item (href="/analytics") from TAB_ITEMS array
    - Keep: Home, Practice, Test, Cards (4 items)
    - Per Pitfall 6: this prevents 404 errors on mobile
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchUserAnalytics" src/app/\(app\)/dashboard/page.tsx
    - grep -q "ReadinessGauge" src/app/\(app\)/dashboard/page.tsx
    - grep -q "CategoryGrid" src/app/\(app\)/dashboard/page.tsx
    - grep -q "QuizHistoryList" src/app/\(app\)/dashboard/page.tsx
    - grep -q "EmptyState" src/app/\(app\)/dashboard/page.tsx
    - grep -vq "/analytics" src/components/layout/sidebar.tsx (Analytics nav item removed)
    - grep -vq "/analytics" src/components/layout/bottom-tabs.tsx (Analytics tab removed)
    - npx tsc --noEmit exits cleanly
  </acceptance_criteria>
  <done>Dashboard page fully wired to analytics data, showing readiness gauge hero, category grid, and quiz history. Analytics nav items removed from sidebar and bottom tabs. No 404 risk for /analytics.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit (no TypeScript errors across all new and modified files)
- grep -rq "RadialBarChart" src/components/dashboard/ (gauge uses Recharts)
- grep -rq "LineChart" src/components/dashboard/ (sparkline uses Recharts)
- grep -rq "fetchUserAnalytics" src/app/\(app\)/dashboard/ (server action wired)
- grep -vq "/analytics" src/components/layout/sidebar.tsx (nav cleaned up)
- grep -vq "/analytics" src/components/layout/bottom-tabs.tsx (tabs cleaned up)
</verification>

<success_criteria>
1. Dashboard page is a server component that fetches analytics and passes to client chart components
2. Readiness gauge shows as a semi-circular ring with color shifting and encouraging message
3. Category cards display mastery %, sparkline trend, and question count with color coding
4. Tapping a category card navigates to /practice?category={slug} for drill mode
5. Weakest categories are visually highlighted
6. Empty state handles both unauthenticated and no-data scenarios gracefully
7. Analytics nav entries removed from sidebar and bottom tabs
8. All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-category-intelligence/03-02-SUMMARY.md`
</output>
