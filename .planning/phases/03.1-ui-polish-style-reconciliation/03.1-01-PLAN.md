---
phase: 03.1-ui-polish-style-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/app/globals.css
  - src/components/layout/app-shell.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Base Card component renders with glass effect and ghost-border by default"
    - "App interior has subtle atmospheric background with radial gradient and blur blob"
    - "bloom-hover respects prefers-reduced-motion"
    - "Bloom hover transitions are 150-300ms per D-07"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card with glass + ghost-border defaults"
      contains: "glass"
    - path: "src/app/globals.css"
      provides: "Updated bloom-hover with reduced-motion support"
      contains: "prefers-reduced-motion"
    - path: "src/components/layout/app-shell.tsx"
      provides: "Atmospheric background with gradient and blur blob"
      contains: "blur"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "src/app/globals.css"
      via: "glass and ghost-border CSS classes"
      pattern: "glass.*ghost-border"
---

<objective>
Update the foundational visual layer: base Card component defaults, CSS utility classes, and app layout atmosphere.

Purpose: Establish the glass-panel HUD aesthetic across the entire app interior by changing defaults at the lowest level (Card component and app layout), so all existing Card usages automatically inherit the cyberpunk treatment.

Output: Card component with glass+ghost-border defaults, updated bloom-hover with accessibility, atmospheric app background.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-CONTEXT.md

<interfaces>
From src/components/ui/card.tsx (current):
- Card uses `bg-card` + `ring-1 ring-foreground/10` as default styling
- Card accepts `className` prop for overrides via cn() merge

From src/app/globals.css (current):
- `.glass` = `backdrop-filter: blur(16px)` + `background-color: color-mix(in srgb, var(--surface-container) 80%, transparent)`
- `.ghost-border` = `border: 1px solid color-mix(in srgb, var(--border) 15%, transparent)`
- `.bloom-hover` = `transition: box-shadow 0.2s ease` + hover `box-shadow: 0 0 20px 2px rgba(255, 174, 216, 0.4)`
- Dark background: `--background: #190f23`

From src/components/layout/app-shell.tsx (current):
- Uses `bg-background` on outer div
- Layout structure: aside (sidebar) + main + BottomTabs

From src/components/landing/hero.tsx (reference aesthetic):
- Blur blobs: `bg-neon-purple/5 blur-[120px]`, `bg-neon-pink/5 blur-[100px]`
- Panel style: `border border-neon-pink/20 bg-card/80 backdrop-blur-2xl rounded-xl`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Card defaults and CSS utilities</name>
  <files>src/components/ui/card.tsx, src/app/globals.css</files>
  <read_first>src/components/ui/card.tsx, src/app/globals.css</read_first>
  <action>
1. In `src/components/ui/card.tsx`, update the Card component's default className:
   - Remove: `bg-card` and `ring-1 ring-foreground/10`
   - Add: `glass ghost-border`
   - Keep all other classes intact (rounded-xl, py-4, text-sm, text-card-foreground, overflow-hidden, group/card, data-slot, data-size, etc.)
   - The final className string for Card should be: `"group/card flex flex-col gap-4 overflow-hidden rounded-xl py-4 text-sm text-card-foreground glass ghost-border has-data-[slot=card-footer]:pb-0 has-[>img:first-child]:pt-0 data-[size=sm]:gap-3 data-[size=sm]:py-3 data-[size=sm]:has-data-[slot=card-footer]:pb-0 *:[img:first-child]:rounded-t-xl *:[img:last-child]:rounded-b-xl"`

2. In `src/app/globals.css`, update `.bloom-hover`:
   - Change transition duration from 0.2s to 0.2s (already within 150-300ms range per D-07, keep as-is)
   - Add `@media (prefers-reduced-motion: reduce)` block that disables the hover box-shadow transition:
     ```css
     @media (prefers-reduced-motion: reduce) {
       .bloom-hover {
         transition: none;
       }
       .bloom-hover:hover {
         box-shadow: none;
       }
     }
     ```
   - Place this immediately after the existing `.bloom-hover:hover` block

3. In `src/app/globals.css`, update `.glass` class to use `bg-card/80` instead of `color-mix` for consistency with the landing page hero panel aesthetic:
   - Change `.glass` to:
     ```css
     .glass {
       backdrop-filter: blur(16px);
       -webkit-backdrop-filter: blur(16px);
       background-color: oklch(from var(--card) l c h / 0.8);
     }
     ```
   - This matches the landing hero's `bg-card/80 backdrop-blur-2xl` approach per D-03

Note: Components that currently add `glass ghost-border` explicitly alongside Card (like quiz-stats.tsx `<Card className="border-0 glass ghost-border">`) will now double up since Card has glass+ghost-border by default. This is fine for now — the `border-0` override and duplicate class names are harmless. Plan 02 will clean those up.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "glass ghost-border" src/components/ui/card.tsx && grep -c "prefers-reduced-motion" src/app/globals.css && grep -c "oklch(from var(--card)" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "glass ghost-border" src/components/ui/card.tsx returns match
    - grep "prefers-reduced-motion" src/app/globals.css returns match
    - grep "oklch(from var(--card)" src/app/globals.css returns match
    - Card no longer contains "bg-card" or "ring-1 ring-foreground/10" in default className
  </acceptance_criteria>
  <done>Card component defaults to glass+ghost-border. bloom-hover respects prefers-reduced-motion. Glass class uses card color at 80% opacity matching landing aesthetic.</done>
</task>

<task type="auto">
  <name>Task 2: Add atmospheric background to app layout</name>
  <files>src/components/layout/app-shell.tsx</files>
  <read_first>src/components/layout/app-shell.tsx, src/components/landing/hero.tsx</read_first>
  <action>
In `src/components/layout/app-shell.tsx`, add atmospheric depth to the app layout per D-13, D-14, D-15, D-16:

1. Update the outer div to include a radial gradient background and a fixed blur blob:
   - Change: `<div className="flex min-h-screen bg-background">`
   - To: `<div className="relative flex min-h-screen bg-background">`

2. Add two atmospheric elements inside the div, before the aside:
   - A radial gradient overlay (D-13): `<div className="pointer-events-none fixed inset-0 -z-10 bg-[radial-gradient(ellipse_at_top,_oklch(0.20_0.04_300)_0%,_transparent_70%)]" />`
   - A fixed blur blob in top-right (D-14, D-15): `<div className="pointer-events-none fixed -top-32 -right-32 -z-10 h-96 w-96 rounded-full bg-neon-purple/[0.04] blur-[120px]" />`

The blob uses:
- `fixed` positioning so it stays in place across all pages (D-15)
- `bg-neon-purple/[0.04]` — extremely subtle so it does not distract during quizzes (D-16)
- `blur-[120px]` matching the landing page's blob blur radius
- `-top-32 -right-32` to position in the top-right corner with natural edge fade
- `-z-10` to sit behind all content
- `pointer-events-none` so it does not interfere with any clicks

The gradient uses:
- Dark purple center (`oklch(0.20 0.04 300)`) fading to transparent — subtle depth (D-13)
- `ellipse_at_top` to feel like atmospheric overhead lighting
- Also `fixed inset-0 -z-10 pointer-events-none`

These are purely decorative background elements. Every page under the (app) layout inherits them automatically.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "blur-\[120px\]" src/components/layout/app-shell.tsx && grep -c "radial-gradient" src/components/layout/app-shell.tsx && grep -c "pointer-events-none" src/components/layout/app-shell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "blur-\[120px\]" src/components/layout/app-shell.tsx returns match
    - grep "radial-gradient" src/components/layout/app-shell.tsx returns match
    - Both atmospheric elements have pointer-events-none and -z-10
    - Both elements use fixed positioning
  </acceptance_criteria>
  <done>App layout has subtle radial gradient and top-right blur blob. All app pages inherit the atmosphere. Elements are behind content (-z-10) and non-interactive (pointer-events-none).</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors (Card changes are backward-compatible)
- Card component renders with backdrop-blur glass effect and subtle ghost-border
- App pages show subtle atmospheric gradient and blur blob in background
- bloom-hover has no transition when prefers-reduced-motion is active
</verification>

<success_criteria>
- Base Card automatically renders as glass HUD panel matching landing page aesthetic
- App interior has atmospheric depth instead of flat background
- Accessibility: bloom effects degrade gracefully for reduced-motion users
- No visual regressions on existing components that already override Card styles
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish-style-reconciliation/03.1-01-SUMMARY.md`
</output>
