---
phase: 03.1-ui-polish-style-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/components/quiz/answer-option.tsx
  - src/components/quiz/question-card.tsx
  - src/components/dashboard/category-card.tsx
  - src/components/dashboard/quiz-history-list.tsx
  - src/components/summary/wrong-answer-carousel.tsx
  - src/components/summary/smart-actions.tsx
  - src/components/summary/quiz-stats.tsx
  - src/components/summary/dmv-result-letter.tsx
autonomous: false

must_haves:
  truths:
    - "Answer options glow pink on hover instead of lavender"
    - "Quiz question card has a neon-cyan left border accent"
    - "Category cards use glass effect with mastery-colored left border"
    - "Dashboard and summary cards use glass instead of surface-container backgrounds"
    - "Interactive elements have bloom-hover treatment"
    - "Non-interactive cards use ghost-border only, no directional accents"
  artifacts:
    - path: "src/components/quiz/answer-option.tsx"
      provides: "Bloom-hover answer options"
      contains: "bloom-hover"
    - path: "src/components/quiz/question-card.tsx"
      provides: "Neon-cyan left border on question card"
      contains: "border-l-2 border-neon-cyan"
    - path: "src/components/dashboard/category-card.tsx"
      provides: "Glass category cards with mastery border"
      contains: "glass ghost-border"
    - path: "src/components/dashboard/quiz-history-list.tsx"
      provides: "Glass quiz history card"
      contains: "glass"
    - path: "src/components/summary/wrong-answer-carousel.tsx"
      provides: "Glass wrong answer cards"
      contains: "glass"
    - path: "src/components/summary/smart-actions.tsx"
      provides: "Glass smart action elements"
      contains: "glass"
  key_links:
    - from: "src/components/quiz/answer-option.tsx"
      to: "src/app/globals.css"
      via: "bloom-hover CSS class"
      pattern: "bloom-hover"
    - from: "src/components/dashboard/category-card.tsx"
      to: "src/components/ui/card.tsx"
      via: "Card component with glass defaults"
      pattern: "Card"
---

<objective>
Apply cyberpunk glass-panel treatment and interaction effects to all app interior components: quiz, dashboard, and summary surfaces.

Purpose: Close the visual gap between the landing page and the app interior. Every card, answer option, and interactive element should feel like a HUD panel from the same design system.

Output: All app components upgraded with glass backgrounds, bloom-hover effects, and directional neon border accents where specified.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-CONTEXT.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-01-SUMMARY.md

<interfaces>
After Plan 01, Card component defaults:
- Card renders with `glass ghost-border` by default (no more bg-card or ring-1)
- Components using Card that add explicit `glass ghost-border` now have redundant classes (harmless but should clean up)

CSS utilities available:
- `.glass` = backdrop-blur + bg-card/80 (updated in Plan 01)
- `.ghost-border` = 1px solid border at 15% opacity
- `.bloom-hover` = pink glow on hover with 0.2s transition, respects prefers-reduced-motion

Neon color variables:
- `--neon-pink` (oklch 0.70 0.30 340 in dark)
- `--neon-cyan` (#a9ffde in dark)
- `--neon-purple` (oklch 0.60 0.28 300 in dark)

Decision constraints:
- D-04: bloom-hover on all interactive elements
- D-05: No persistent glows inside the app
- D-06: Answer options get bloom-hover replacing lavender glow
- D-09: Directional neon borders on KEY interactive cards only (category cards, question card)
- D-10: Category cards use mastery color as left border (already exists)
- D-11: Quiz question card gets neon-cyan left border
- D-12: Non-interactive cards (stats, history) rely on ghost-border only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade quiz components with glass and neon accents</name>
  <files>src/components/quiz/answer-option.tsx, src/components/quiz/question-card.tsx</files>
  <read_first>src/components/quiz/answer-option.tsx, src/components/quiz/question-card.tsx</read_first>
  <action>
**answer-option.tsx** (D-04, D-06):

1. Update the idle variant styling to use glass + bloom-hover instead of surface-container backgrounds:
   - Change the idle variant from:
     `"cursor-pointer border-0 bg-[var(--surface-container-low)] hover:bg-[var(--surface-container-high)] hover:shadow-[0_0_20px_2px_rgba(234,178,255,0.3)] hover:scale-[1.01] active:scale-[0.99]"`
   - To:
     `"cursor-pointer border-0 glass ghost-border bloom-hover hover:scale-[1.01] active:scale-[0.99]"`
   - This replaces the inline lavender glow `rgba(234,178,255,0.3)` with bloom-hover's pink glow per D-06

2. Update the dimmed variant background:
   - Change from: `"border-0 bg-[var(--surface-container-lowest)] opacity-50"`
   - To: `"border-0 bg-card/40 opacity-50"`

3. Update the idle label badge background:
   - Change from: `"bg-[var(--surface-container)] text-muted-foreground"`
   - To: `"bg-card/60 text-muted-foreground"`

4. Update the dimmed label badge background:
   - Change from: `"bg-[var(--surface-container-lowest)] text-muted-foreground/50"`
   - To: `"bg-card/30 text-muted-foreground/50"`

**question-card.tsx** (D-11):

1. Update the question text container to add neon-cyan left border accent:
   - Change from: `"rounded-xl bg-[var(--surface-container-low)] ghost-border p-5"`
   - To: `"rounded-xl glass ghost-border border-l-2 border-neon-cyan p-5"`
   - The `border-l-2 border-neon-cyan` adds the directional accent per D-11
   - The `ghost-border` class adds the subtle all-around border; `border-l-2` overrides just the left side with the neon accent
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "bloom-hover" src/components/quiz/answer-option.tsx && grep -c "border-l-2 border-neon-cyan" src/components/quiz/question-card.tsx && grep -c "surface-container" src/components/quiz/answer-option.tsx | grep "^0$" || echo "WARN: surface-container refs remain in answer-option"</automated>
  </verify>
  <acceptance_criteria>
    - grep "bloom-hover" src/components/quiz/answer-option.tsx returns at least 1 match
    - grep "border-l-2 border-neon-cyan" src/components/quiz/question-card.tsx returns 1 match
    - grep "surface-container" src/components/quiz/answer-option.tsx returns 0 matches
    - grep "surface-container" src/components/quiz/question-card.tsx returns 0 matches
  </acceptance_criteria>
  <done>Answer options use bloom-hover pink glow instead of lavender. Question card has neon-cyan left border accent. Both use glass backgrounds instead of surface-container vars.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade dashboard and summary components with glass treatment</name>
  <files>src/components/dashboard/category-card.tsx, src/components/dashboard/quiz-history-list.tsx, src/components/summary/wrong-answer-carousel.tsx, src/components/summary/smart-actions.tsx, src/components/summary/quiz-stats.tsx, src/components/summary/dmv-result-letter.tsx</files>
  <read_first>src/components/dashboard/category-card.tsx, src/components/dashboard/quiz-history-list.tsx, src/components/summary/wrong-answer-carousel.tsx, src/components/summary/smart-actions.tsx, src/components/summary/quiz-stats.tsx, src/components/summary/dmv-result-letter.tsx</read_first>
  <action>
**category-card.tsx** (D-09, D-10):

1. Update Card className to use glass defaults + bloom-hover:
   - Change from: `"group border-0 bg-[var(--surface-container-low)] transition-all hover:bg-[var(--surface-container-high)] hover:shadow-[0_0_20px_2px_rgba(255,174,216,0.15)] cursor-pointer h-full"`
   - To: `"group transition-all bloom-hover cursor-pointer h-full"`
   - Card already has glass+ghost-border from Plan 01 defaults. Remove explicit bg and hover overrides.
   - The mastery color left border via inline `style={{ borderLeft }}` stays as-is (D-10 already satisfied)

**quiz-history-list.tsx** (D-12 -- non-interactive, ghost-border only):

1. Update Card className:
   - Change from: `"border-0 bg-[var(--surface-container-low)]"`
   - To: `""` (empty string -- Card defaults from Plan 01 already provide glass+ghost-border)
   - This is a non-interactive card, so NO bloom-hover per D-12

2. Update the row hover background:
   - Change from: `"hover:bg-[var(--surface-container-high)]"`
   - To: `"hover:bg-card/60"`

**wrong-answer-carousel.tsx**:

1. Update the "Perfect Score" empty state container:
   - Change from: `"rounded-xl bg-[var(--surface-container-low)] ghost-border p-8 text-center"`
   - To: `"rounded-xl glass ghost-border p-8 text-center"`

2. Update the wrong answer card container:
   - Change from: `"rounded-xl bg-[var(--surface-container-low)] ghost-border p-5"`
   - To: `"rounded-xl glass ghost-border p-5"`

**smart-actions.tsx**:

1. Update the weakest category drill recommendation card:
   - Change from: `"relative overflow-hidden rounded-xl bg-[var(--surface-container-low)] ghost-border p-4 transition-all bloom-hover"`
   - To: `"relative overflow-hidden rounded-xl glass ghost-border p-4 transition-all bloom-hover"`

2. Update the two action buttons:
   - Change from: `"h-12 w-full font-ui text-sm ghost-border border-0 bg-[var(--surface-container-low)] hover:bg-[var(--surface-container-high)]"`
   - To: `"h-12 w-full font-ui text-sm ghost-border border-0 glass hover:bg-card/60"`
   - Apply to BOTH the "Try Again" and "Dashboard" buttons

**quiz-stats.tsx** (D-12 -- non-interactive stats):

1. Clean up redundant classes now that Card has glass+ghost-border by default:
   - Change from: `"border-0 glass ghost-border"`
   - To: `""` (empty string -- Card defaults handle it)

**dmv-result-letter.tsx**:

1. Update the letter container:
   - Change from: `"rounded-xl glass ghost-border bg-[#faf9f6] dark:bg-[var(--surface-container)]/90 p-8"`
   - To: `"rounded-xl glass ghost-border bg-[#faf9f6] dark:bg-card/90 p-8"`
   - Only replacing the dark mode surface-container reference; glass is already present
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && echo "category-card:" && grep -c "surface-container" src/components/dashboard/category-card.tsx && echo "quiz-history:" && grep -c "surface-container" src/components/dashboard/quiz-history-list.tsx && echo "wrong-answer:" && grep -c "surface-container" src/components/summary/wrong-answer-carousel.tsx && echo "smart-actions:" && grep -c "surface-container" src/components/summary/smart-actions.tsx && echo "quiz-stats:" && grep -c "surface-container" src/components/summary/quiz-stats.tsx && echo "dmv-letter:" && grep -c "surface-container" src/components/summary/dmv-result-letter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "surface-container" returns 0 matches in ALL six files listed above
    - grep "bloom-hover" src/components/dashboard/category-card.tsx returns 1 match (interactive card)
    - grep "bloom-hover" src/components/dashboard/quiz-history-list.tsx returns 0 matches (non-interactive per D-12)
    - grep "glass" src/components/summary/wrong-answer-carousel.tsx returns at least 1 match
  </acceptance_criteria>
  <done>All dashboard and summary components use glass backgrounds. Interactive cards have bloom-hover. Non-interactive cards (history, stats) use ghost-border only. Zero surface-container var references remain in these files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full style reconciliation</name>
  <action>Human visual review of the complete UI polish across all app screens.</action>
  <verify>User confirms visual quality by typing "approved"</verify>
  <done>User has verified the visual reconciliation meets expectations</done>
  <what-built>
    Full visual reconciliation of the app interior with the landing page cyberpunk aesthetic:
    1. All cards render as glass HUD panels with backdrop-blur and subtle borders
    2. App background has atmospheric radial gradient and blur blob
    3. Answer options glow pink on hover (not lavender)
    4. Question card has neon-cyan left border accent
    5. Category cards have mastery-colored left borders with glass treatment
    6. Interactive elements have bloom-hover, non-interactive cards do not
    7. prefers-reduced-motion disables bloom effects
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Navigate from landing page into /dashboard -- verify the visual "drop" is reduced, cards feel like glass HUD panels
    3. Check background: should see subtle purple gradient at top and a faint blur blob in the upper-right
    4. On dashboard: verify category cards have glass effect + mastery color left border + pink glow on hover
    5. On dashboard: verify quiz history card has glass effect but NO hover glow (non-interactive)
    6. Click into a practice quiz: verify question card has neon-cyan left border accent
    7. Hover over answer options: verify pink glow appears (not lavender)
    8. Complete a quiz and check summary page: stats card, wrong answer cards, and smart actions all use glass
    9. Test on mobile viewport width: verify everything still looks good, blob is not distracting
    10. (Optional) Test prefers-reduced-motion in browser DevTools: bloom-hover should be disabled
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Zero `surface-container` references remain in the modified component files
- All modified components use glass/ghost-border CSS classes
- Interactive cards use bloom-hover, non-interactive cards do not
- Visual checkpoint confirms cohesive cyberpunk aesthetic between landing and app interior
</verification>

<success_criteria>
- Walking from landing page to app interior feels like the same design system
- Cards throughout the app look like floating glass HUD panels
- Hover effects are consistently pink bloom across all interactive elements
- Question card and category cards have appropriate directional neon border accents
- Background atmosphere adds depth without distracting from quiz content
- No visual fatigue from persistent glows (D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish-style-reconciliation/03.1-02-SUMMARY.md`
</output>
