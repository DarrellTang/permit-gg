---
phase: 03.1-ui-polish-style-reconciliation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/layout/sidebar.tsx
  - src/components/layout/app-shell.tsx
  - src/components/layout/bottom-tabs.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Main content area has a subtle cyan grid line overlay visible behind content"
    - "Sidebar has a right border accent with primary/15 opacity"
    - "Active sidebar item shows a border-r-4 neon-pink right accent instead of bottom shadow"
    - "Inactive sidebar items shift right on hover with translate-x-1 transition"
    - "Settings and Logout are separate labeled buttons at the bottom of the sidebar"
    - "bloom-primary and bloom-secondary CSS utility classes exist for drop-shadow effects"
  artifacts:
    - path: "src/app/globals.css"
      provides: "bg-grid, bloom-primary, bloom-secondary CSS utility classes"
      contains: "bg-grid"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Redesigned sidebar with right border accent, bottom Settings/Logout"
    - path: "src/components/layout/app-shell.tsx"
      provides: "bg-grid class on main content area"
  key_links:
    - from: "src/components/layout/app-shell.tsx"
      to: "src/app/globals.css"
      via: "bg-grid class usage"
      pattern: "bg-grid"
    - from: "src/components/layout/sidebar.tsx"
      to: "src/server/actions/auth.ts"
      via: "signOut server action for logout button"
      pattern: "signOut"
---

<objective>
Add StitchU-inspired CSS utilities (bg-grid, bloom-primary, bloom-secondary) and redesign the sidebar with right-border active accents, hover translate effects, and dedicated Settings/Logout buttons at the bottom.

Purpose: Incorporate the StitchU redesign's foundational visual elements — the grid background overlay and sidebar interaction patterns — while keeping Permit.GG branding and existing navigation structure.

Output: Updated globals.css with new utility classes, redesigned sidebar component, bg-grid overlay on main content.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-CONTEXT.md

<interfaces>
<!-- Existing signOut server action used by sidebar logout -->
From src/server/actions/auth.ts:
```typescript
export async function signOut(): Promise<void>;
```

From src/components/layout/sidebar.tsx:
```typescript
interface SidebarProps {
  displayName?: string
  avatarUrl?: string
  email?: string
}
```

From src/components/layout/app-shell.tsx:
```typescript
interface AppShellProps {
  children: React.ReactNode
  displayName?: string
  avatarUrl?: string
  email?: string
}
```

Existing CSS classes in globals.css:
- `.glass` — backdrop-blur + oklch card bg at 80% opacity
- `.ghost-border` — 1px border at 15% opacity
- `.bloom-hover` — pink glow box-shadow on hover
- `.neon-glow` — multi-layer pink box-shadow (persistent)
- `.neon-text` — text-shadow with pink/purple
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS utility classes and grid background to app shell</name>
  <files>src/app/globals.css, src/components/layout/app-shell.tsx, src/components/layout/bottom-tabs.tsx</files>
  <action>
In globals.css, add after the existing `.data-leak` block:

1. Add `.bg-grid` class — a subtle cyan grid line overlay:
```css
.bg-grid {
  background-image: linear-gradient(to right, rgba(169, 255, 222, 0.05) 1px, transparent 1px),
                    linear-gradient(to bottom, rgba(169, 255, 222, 0.05) 1px, transparent 1px);
  background-size: 40px 40px;
}
```

2. Add `.bloom-primary` class — pink drop-shadow filter:
```css
.bloom-primary {
  filter: drop-shadow(0 0 8px rgba(255, 174, 216, 0.4));
}
```

3. Add `.bloom-secondary` class — mint/cyan drop-shadow filter:
```css
.bloom-secondary {
  filter: drop-shadow(0 0 8px rgba(169, 255, 222, 0.4));
}
```

In app-shell.tsx:
- Add `bg-grid` class to the `<main>` element alongside existing classes so the grid overlay appears behind all page content. The main element becomes: `className="flex-1 overflow-y-auto pb-20 lg:pb-0 bg-grid"`

In bottom-tabs.tsx:
- Replace `bg-[var(--surface-container)]/80` with `glass` class to match the sidebar's backdrop-blur treatment and eliminate surface-container var usage. The outer div becomes: `className="fixed inset-x-0 bottom-0 z-50 glass lg:hidden"` (glass already provides backdrop-blur + card bg at 80% opacity).
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "bg-grid" src/app/globals.css src/components/layout/app-shell.tsx && grep -c "bloom-primary" src/app/globals.css && grep -c "bloom-secondary" src/app/globals.css && grep -c "glass" src/components/layout/bottom-tabs.tsx</automated>
  </verify>
  <done>bg-grid class defined in CSS and applied to main content area. bloom-primary and bloom-secondary utility classes exist. Bottom tabs use glass class instead of surface-container var.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign sidebar with right-border accents and Settings/Logout bottom section</name>
  <files>src/components/layout/sidebar.tsx</files>
  <action>
Redesign the sidebar component with these StitchU-inspired changes while keeping all existing NAV_ITEMS, Permit.GG branding, and the signOut server action:

**Outer container:**
- Replace `bg-[var(--surface-container)]/80 backdrop-blur-[16px]` with `glass` class (consistent with bottom-tabs and card treatment)
- Add right border: `border-r border-neon-pink/15`

**Nav items — active state:**
- Remove the current bottom-shadow approach (`shadow-[0_-2px_0_0_var(--neon-pink)_inset] shadow-neon-pink/60`)
- Replace with right-border accent: `border-r-4 border-neon-pink bg-neon-pink/10 text-neon-pink`
- The item itself should NOT have rounded corners on the right side (use `rounded-l-lg rounded-r-none`) so the right border accent sits flush against the sidebar's right edge

**Nav items — inactive state:**
- Use `text-muted-foreground/60` for the dimmed inactive look
- On hover: `hover:text-foreground hover:translate-x-1` for the rightward shift effect
- Add `transition-all duration-200` for smooth hover transitions

**Icon treatment on active:**
- Active icon gets `bloom-primary` filter class for the pink drop-shadow glow
- Inactive icons: just inherit text color

**Bottom section redesign:**
Replace the current compact user-info + tiny sign-out button layout with a proper bottom section:

1. Keep the user avatar/name display at the top of the bottom section (same as current)
2. Add a divider: `<div className="my-2 border-t border-neon-pink/10" />`
3. Add a Settings button as a full-width nav-like link: `<Link href="/dashboard" className="...">` (link to /dashboard for now since no settings page exists; placeholder) with a gear SVG icon and "Settings" label. Style it the same as inactive nav items with hover translate-x-1 effect.
4. Add a Logout button as a full-width form submit button (using the existing signOut server action form pattern): gear icon replaced with the existing logout SVG icon, "Sign Out" label, same styling as Settings but using `text-neon-pink/60 hover:text-neon-pink` for destructive emphasis.
5. Keep ThemeToggle at the very bottom

**Bottom section container:**
- Replace `bg-[var(--surface-container-low)]` with `border-t border-neon-pink/10` (use border instead of background change for separation)
- Add `space-y-1 px-3 py-3`

The Settings gear SVG icon (inline, 20x20):
```tsx
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" className="h-5 w-5">
  <path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z" />
  <circle cx="12" cy="12" r="3" />
</svg>
```

Keep the existing log-out SVG icon already in the file for the Sign Out button. The overall layout: PERMIT.GG logo at top, nav items in the middle (flex-1), then divider + user info + settings + sign out + theme toggle at the bottom.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "border-r-4" src/components/layout/sidebar.tsx && grep -c "translate-x-1" src/components/layout/sidebar.tsx && grep -c "Settings" src/components/layout/sidebar.tsx && grep -c "Sign Out" src/components/layout/sidebar.tsx && grep -c "bloom-primary" src/components/layout/sidebar.tsx && npx next build --no-lint 2>&1 | tail -5</automated>
  </verify>
  <done>Sidebar has right-border accent on active items, hover translate-x-1 on inactive items, dedicated Settings and Sign Out buttons at bottom, and bloom-primary on active icons. Build succeeds.</done>
</task>

</tasks>

<verification>
- `grep "bg-grid" src/app/globals.css` returns the grid CSS definition
- `grep "bg-grid" src/components/layout/app-shell.tsx` confirms it is applied to main content
- `grep "bloom-primary" src/app/globals.css` returns the pink drop-shadow definition
- `grep "bloom-secondary" src/app/globals.css` returns the mint drop-shadow definition
- `grep "border-r-4" src/components/layout/sidebar.tsx` confirms right-border accent on active nav items
- `grep "Settings" src/components/layout/sidebar.tsx` confirms Settings button exists
- `grep "Sign Out" src/components/layout/sidebar.tsx` confirms labeled Logout button exists
- `npx next build --no-lint` succeeds without errors
</verification>

<success_criteria>
1. Grid background overlay is visible on the main content area (subtle cyan lines at 40px intervals)
2. bloom-primary and bloom-secondary CSS classes available for use by other components
3. Sidebar active item uses border-r-4 right accent in neon-pink instead of bottom shadow
4. Inactive sidebar items shift right on hover
5. Settings and Sign Out are distinct, labeled buttons at the bottom of the sidebar
6. Bottom tabs use glass class instead of surface-container variable
7. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish-style-reconciliation/03.1-04-SUMMARY.md`
</output>
