---
phase: 03.1-ui-polish-style-reconciliation
plan: 03
type: execute
wave: 1
depends_on: ["03.1-02"]
files_modified:
  - src/components/ui/card.tsx
  - src/components/quiz/answer-option.tsx
  - src/components/quiz/question-card.tsx
  - src/app/(marketing)/login/page.tsx
  - src/components/quiz/quiz-shell.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/components/dashboard/empty-state.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Cards and answer options use rounded-lg (sharper) instead of rounded-xl (soft)"
    - "Quiz question card has a clearly visible neon-cyan left border accent"
    - "Login OAuth buttons have glass background, ghost-border, and bloom-hover"
    - "Primary CTA buttons (Start Practice, Start Drill, Try Sim Test) have neon-glow effect"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card with rounded-lg default"
      contains: "rounded-lg"
    - path: "src/components/quiz/answer-option.tsx"
      provides: "Answer options with rounded-lg"
      contains: "rounded-lg"
    - path: "src/components/quiz/question-card.tsx"
      provides: "Question card with border-l-4 neon-cyan"
      contains: "border-l-4"
    - path: "src/app/(marketing)/login/page.tsx"
      provides: "OAuth buttons with glass treatment"
      contains: "glass"
    - path: "src/components/quiz/quiz-shell.tsx"
      provides: "Start Practice/Drill button with neon-glow"
      contains: "neon-glow"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "all Card consumers"
      via: "rounded-lg default class"
      pattern: "rounded-lg"
---

<objective>
Close 5 UAT gaps from Phase 3.1 visual polish: sharpen border radii (rounded-xl to rounded-lg on cards and answer options), increase question card neon-cyan border prominence, add cyberpunk glass treatment to login OAuth buttons, and add neon-glow to primary CTA buttons.

Purpose: UAT revealed visual inconsistencies between the landing page's sharp cyberpunk aesthetic and remaining soft/flat elements in the app interior.
Output: All 5 UAT gaps resolved; app interior matches landing page design language.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-CONTEXT.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-UAT.md
@.planning/phases/03.1-ui-polish-style-reconciliation/03.1-02-SUMMARY.md

<interfaces>
From src/app/globals.css (existing utility classes to use):
```css
.neon-glow {
  box-shadow:
    0 0 10px var(--neon-pink),
    0 0 40px oklch(from var(--neon-pink) l c h / 0.3);
}

.glass {
  backdrop-filter: blur(24px);
  background-color: oklch(from var(--card) l c h / 0.8);
}

.bloom-hover {
  transition: box-shadow 0.2s ease;
}
.bloom-hover:hover {
  box-shadow: 0 0 20px 2px rgba(255, 174, 216, 0.4);
}

.ghost-border {
  border: 1px solid oklch(from var(--foreground) l c h / 0.15);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sharpen border radii and increase question card border</name>
  <files>src/components/ui/card.tsx, src/components/quiz/answer-option.tsx, src/components/quiz/question-card.tsx</files>
  <action>
Fix 3 UAT gaps (Tests 4, 5, 6) by sharpening border radii and increasing neon border visibility.

**card.tsx** (line 15): Change `rounded-xl` to `rounded-lg` in the Card component's base className. This is the single change that makes ALL cards sharper across the app. Also update the image border-radius utilities on the same line: `*:[img:first-child]:rounded-t-xl` to `*:[img:first-child]:rounded-t-lg` and `*:[img:last-child]:rounded-b-xl` to `*:[img:last-child]:rounded-b-lg`.

**answer-option.tsx** (line 61): Change `rounded-xl` to `rounded-lg` in the base className of the motion.button element. This makes answer options sharper to match the cyberpunk HUD panel feel.

**question-card.tsx** (line 52): Change `border-l-2` to `border-l-4` on the question card div to make the neon-cyan left accent visually prominent. The color `border-neon-cyan` stays the same.

Do NOT touch any `rounded-full` on spinners, progress bars, or avatars -- those are circular elements that should stay fully rounded. Only change `rounded-xl` on card/panel/option surfaces.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "rounded-xl" src/components/ui/card.tsx && grep -c "rounded-xl" src/components/quiz/answer-option.tsx && grep "border-l-4" src/components/quiz/question-card.tsx | head -1</automated>
  </verify>
  <done>card.tsx shows 0 occurrences of rounded-xl, answer-option.tsx shows 0 occurrences of rounded-xl, question-card.tsx contains border-l-4 border-neon-cyan</done>
</task>

<task type="auto">
  <name>Task 2: Cyberpunk OAuth buttons and neon-glow on primary CTAs</name>
  <files>src/app/(marketing)/login/page.tsx, src/components/quiz/quiz-shell.tsx, src/app/(app)/dashboard/page.tsx, src/components/dashboard/empty-state.tsx</files>
  <action>
Fix 2 UAT gaps (Tests 7, 8) by adding cyberpunk treatment to OAuth buttons and neon-glow to primary CTAs.

**login/page.tsx — OAuth buttons (lines 109-131):**
- Google button (line 112): Replace `bg-white` flat styling with glass treatment. Change className to: `"flex w-full items-center justify-center gap-3 glass ghost-border rounded-lg px-6 py-4 font-ui text-sm font-bold tracking-wide text-foreground transition-all bloom-hover active:scale-[0.98] disabled:opacity-50"`. Remove the flat white background. The Google SVG icon colors remain unchanged.
- Discord button (lines 118-130): Remove inline `style={{ backgroundColor: "#5865F2" }}`. Change className to: `"flex w-full items-center justify-center gap-3 glass ghost-border rounded-lg bg-[#5865F2]/20 px-6 py-4 font-ui text-sm font-bold tracking-wide text-[#5865F2] transition-all bloom-hover active:scale-[0.98] disabled:opacity-50"`. This gives it the glass background with a hint of Discord purple, consistent with the cyberpunk card style.

**quiz-shell.tsx — Start Practice/Start Drill button (line 232):**
Add `neon-glow` class to the existing className string. The button already has the gradient and shadow, just add the glow. Change className to: `"neon-glow h-12 w-full bg-gradient-to-r from-neon-purple to-neon-cyan font-ui text-base font-bold text-white shadow-lg shadow-neon-purple/20 transition-all hover:shadow-neon-cyan/30"`

**dashboard/page.tsx — CTA links (lines 48-59 and lines 79-92):**
- "Start Practice" link (line 50): Add `neon-glow` class. Change to: `"neon-glow inline-block rounded-lg bg-neon-cyan/20 px-4 py-2 font-ui text-sm font-semibold text-neon-cyan transition-all hover:bg-neon-cyan/30"`
- "Try Sim Test" link (line 56): Add `neon-glow` class. Change to: `"neon-glow inline-block rounded-lg bg-neon-purple/20 px-4 py-2 font-ui text-sm font-semibold text-neon-purple transition-all hover:bg-neon-purple/30"`
- "Quick Practice" link (line 82): Add `neon-glow` class similarly.
- "Sim Test" link (line 88): Add `neon-glow` class similarly.

**empty-state.tsx — CTA links (lines 67-79):**
- "Start Practice" link (line 69): Add `neon-glow` class. Change to: `"neon-glow inline-block rounded-lg bg-neon-cyan/20 px-5 py-3 font-ui text-sm font-semibold text-neon-cyan transition-all hover:bg-neon-cyan/30"`
- "Try Sim Test" link (line 75): Add `neon-glow` class. Change to: `"neon-glow inline-block rounded-lg bg-neon-purple/20 px-5 py-3 font-ui text-sm font-semibold text-neon-purple transition-all hover:bg-neon-purple/30"`
- "Sign In" link in unauthenticated variant (line 36): Add `neon-glow` class similarly.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/codeWorkspace/permit-gg && grep -c "neon-glow" src/components/quiz/quiz-shell.tsx && grep -c "neon-glow" src/app/\(app\)/dashboard/page.tsx && grep -c "neon-glow" src/components/dashboard/empty-state.tsx && grep -c "glass" src/app/\(marketing\)/login/page.tsx</automated>
  </verify>
  <done>quiz-shell.tsx has 1+ neon-glow occurrence on Start button, dashboard/page.tsx has 4 neon-glow occurrences on CTA links, empty-state.tsx has 3 neon-glow occurrences, login/page.tsx has 2+ glass occurrences on OAuth buttons with no inline backgroundColor style</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors (no broken imports or class issues)
2. All 5 UAT gap truths are now satisfied:
   - GAP 1: Cards use rounded-lg globally via card.tsx default
   - GAP 2: Answer options use rounded-lg
   - GAP 3: Question card border-l-4 is visually prominent
   - GAP 4: OAuth buttons have glass + ghost-border + bloom-hover
   - GAP 5: Primary CTAs have neon-glow class
</verification>

<success_criteria>
- Zero `rounded-xl` in card.tsx and answer-option.tsx
- `border-l-4` in question-card.tsx
- `glass` and `ghost-border` on both OAuth buttons in login/page.tsx
- `neon-glow` on Start Practice/Drill button in quiz-shell.tsx
- `neon-glow` on all 4 dashboard CTA links and all 3 empty-state CTA links
- `npx next build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish-style-reconciliation/03.1-03-SUMMARY.md`
</output>
