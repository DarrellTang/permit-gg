---
phase: 01-foundation-question-bank
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
  - src/app/page.tsx
  - src/components/landing/hero.tsx
  - src/components/landing/features.tsx
  - src/components/landing/cta.tsx
  - src/components/landing/social-proof.tsx
  - src/components/mascot/mascot.tsx
autonomous: false
requirements:
  - UI-03

must_haves:
  truths:
    - "Landing page loads at root URL / with marketing layout (no sidebar/tabs)"
    - "Hero section has compelling headline, subheadline, and prominent Start Studying CTA"
    - "CTA button navigates to /dashboard (the app)"
    - "Landing page uses the Magical Girl Cyberpunk aesthetic with neon colors and animations"
    - "Mascot character is present on the landing page"
    - "Page is fully responsive on mobile and desktop"
    - "Human has visually verified the cyberpunk aesthetic, responsiveness, and dark mode"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Landing page at root URL /"
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Marketing layout (no app shell)"
    - path: "src/components/landing/hero.tsx"
      provides: "Hero section with headline and CTA"
      exports: ["Hero"]
    - path: "src/components/landing/features.tsx"
      provides: "Feature highlights section"
      exports: ["Features"]
    - path: "src/components/mascot/mascot.tsx"
      provides: "Cyberpunk mascot character component"
      exports: ["Mascot"]
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/landing/hero.tsx"
      via: "Page composes landing sections"
      pattern: "Hero|Features|CTA"
    - from: "src/components/landing/hero.tsx"
      to: "/dashboard"
      via: "Start Studying CTA links to app"
      pattern: "href.*dashboard"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/app/layout.tsx"
      via: "Nested under root layout (shares fonts + ThemeProvider)"
      pattern: "children"
---

<objective>
Build the marketing landing page at the root URL with compelling copy, the Magical Girl Cyberpunk aesthetic, and a mascot character. Then visually verify the entire Phase 1 UI.

Purpose: The landing page is the first thing users see. It must build demand, communicate the value prop, and funnel to the app. The visual checkpoint ensures the cyberpunk aesthetic meets the creative vision.
Output: A polished landing page with hero, features, CTA, and mascot. Human-verified UI quality.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-question-bank/01-CONTEXT.md
@.planning/phases/01-foundation-question-bank/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 - theming system -->
globals.css provides: bg-primary, text-primary, bg-card, border-border, neon-pink, neon-purple, neon-cyan
Font classes: font-display (Orbitron), font-ui (Chakra Petch), font-body (Inter)
Utility classes: .neon-glow, .neon-text

<!-- From Plan 03 - components that exist -->
From src/components/layout/theme-toggle.tsx:
```typescript
export function ThemeToggle(): JSX.Element
```

Landing page does NOT use AppShell (no sidebar/tabs). It has its own minimal layout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build marketing landing page with cyberpunk aesthetic and mascot</name>
  <files>src/app/(marketing)/layout.tsx, src/app/(marketing)/page.tsx, src/app/page.tsx, src/components/landing/hero.tsx, src/components/landing/features.tsx, src/components/landing/cta.tsx, src/components/landing/social-proof.tsx, src/components/mascot/mascot.tsx</files>
  <action>
    1. Remove the redirect from `src/app/page.tsx` (Plan 03 added a redirect to /dashboard). Delete this file entirely -- the (marketing) route group will handle the root URL.

    2. Create `src/app/(marketing)/layout.tsx`:
       - Minimal layout: no sidebar, no bottom tabs
       - Just `<div className="min-h-screen bg-background">{children}</div>`
       - Optionally add a thin top nav with "PERMIT.GG" wordmark and ThemeToggle
       - This is nested under root layout (fonts + ThemeProvider already applied)

    3. Create `src/components/mascot/mascot.tsx`:
       - "use client" component
       - SVG-based Magical Girl Cyberpunk mascot character
       - Design: feminine cyberpunk aesthetic (Bubblegum Crisis inspired) -- think angular armor/outfit with glowing neon accents, energy visor or headset, dynamic pose suggesting confidence
       - Use inline SVG with neon colors from theme (primary pink, accent cyan, secondary purple)
       - Accept props: `size?: "sm" | "md" | "lg"`, `pose?: "neutral" | "encouraging" | "celebrating"`
       - Poses can be subtle variations (different arm position, expression change) -- keep it achievable with SVG path differences
       - Add subtle CSS animation: gentle float/bob effect using Motion or CSS keyframes
       - If generating a detailed SVG is too complex, create a stylized geometric/abstract character that captures the cyberpunk energy (angular shapes, neon outlines, glowing accents) -- quality over complexity

    4. Create `src/components/landing/hero.tsx`:
       - "use client" (uses Motion for animations)
       - Large headline in Orbitron (font-display): compelling copy like "Crush Your CA Permit Test" or "Level Up Your Driving Knowledge" -- use marketing psychology (confidence, achievement framing)
       - Subheadline in Chakra Petch (font-ui): "AI-powered practice that adapts to how you learn. Not your boring practice test."
       - Prominent "Start Studying" CTA button: large, neon-pink bg, glow effect on hover, links to /dashboard
       - Mascot character positioned alongside or below the headline
       - Motion entrance animations: headline slides up, subheadline fades in, CTA bounces in
       - Neon gradient background effects (subtle, not overwhelming)
       - Mobile: stack vertically, full-width CTA
       - Desktop: hero content left, mascot right (or centered layout)

    5. Create `src/components/landing/features.tsx`:
       - 3-4 feature highlight cards:
         - "AI Question Variations" -- every session is different, no memorizing answers
         - "Know Your Weak Spots" -- category analytics show exactly where to focus
         - "Simulated DMV Test" -- 46 questions, real format, know if you're ready
         - "Spaced Repetition" -- science-backed review scheduling
       - Each card: icon/emoji, heading in Chakra Petch, description in Inter
       - Cards use cyberpunk styling: dark card bg, neon border accent, hover glow
       - Grid layout: 2x2 on desktop, stack on mobile
       - Motion: cards animate in with stagger when scrolled into view

    6. Create `src/components/landing/social-proof.tsx`:
       - Since no real users yet, use positioning copy:
         - "Built for California teens" with the 8 category badges
         - "300+ verified questions from the official CA DMV Handbook"
         - "Pass rate: 83% needed -- we'll get you there"
       - Use loss aversion psychology: "67% of teens fail the first time. Don't be one of them." (or similar factual framing)
       - Cyberpunk-styled stat counters or badges

    7. Create `src/components/landing/cta.tsx`:
       - Bottom CTA section (above footer)
       - Compelling closing: "Ready to pass?" or "Your permit is waiting"
       - Large CTA button matching hero style
       - Mascot in encouraging pose

    8. Create `src/app/(marketing)/page.tsx`:
       - Server Component that composes: Hero, Features, SocialProof, CTA sections
       - Smooth scroll between sections
       - Metadata: title "PERMIT.GG - Crush Your CA Permit Test", description for SEO

    9. Verify the app builds and the root URL loads the landing page (not the dashboard).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page loads at root URL with hero, features, social proof, and CTA sections. Mascot character present. All content uses cyberpunk theming. CTA links to /dashboard. Mobile responsive. Builds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Magical Girl Cyberpunk aesthetic</name>
  <action>
    Present the complete Phase 1 UI for human verification. Start the dev server with `npm run dev` and confirm all pages load. Then pause for human review of the visual identity.

    What was built across Plans 01-04:
    - Landing page at root URL with Magical Girl Cyberpunk aesthetic
    - App shell at /dashboard with adaptive sidebar (desktop) / bottom tabs (mobile)
    - Dark mode toggle with light mode alternate
    - Onboarding walkthrough at /onboarding
    - Mascot character on landing page
    - Cyberpunk theming: neon pinks, purples, cyan on dark chrome backgrounds
    - Orbitron display font, Chakra Petch UI font, Inter body font
  </action>
  <verify>
    Human verifies by:
    1. Run `npm run dev` and open http://localhost:3000 in Chrome
    2. Landing page (root /): Verify cyberpunk aesthetic (neon pinks/purples/cyan on dark background), Orbitron font on headlines, mascot character visible, "Start Studying" CTA prominent with glow effect. Click CTA -- should navigate to /dashboard.
    3. Dashboard (/dashboard): Desktop (>1024px) -- sidebar visible on left with "PERMIT.GG" branding and nav items. Mobile (resize to <1024px) -- sidebar disappears, bottom tab bar appears with adequate touch targets.
    4. Dark/Light mode: Find theme toggle, switch to light mode (colors should invert readably), switch back to dark (neon colors return).
    5. Onboarding (/onboarding): Navigate there, step through walkthrough, verify smooth transitions.
    6. Overall vibe check: Does it feel like Magical Girl Cyberpunk (Bubblegum Crisis inspired)? Gaming-oriented (.gg energy)? Engaging for teens (not sterile/corporate)? Fonts readable?
  </verify>
  <done>Human has reviewed and approved the visual identity as meeting the Magical Girl Cyberpunk creative vision, or has provided specific feedback for iteration.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Root URL / loads marketing landing page (not dashboard)
3. /dashboard loads app shell with navigation
4. Theme toggle works on both marketing and app pages
5. Human approves the cyberpunk aesthetic
</verification>

<success_criteria>
- Landing page at / with hero, features, social proof, CTA sections
- Mascot character visible on landing page
- CTA navigates to /dashboard
- Entire UI uses Magical Girl Cyberpunk aesthetic (neon pinks/purples/cyan on dark chrome)
- Responsive on mobile and desktop
- Human has visually approved the aesthetic as matching the creative vision
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-question-bank/01-04-SUMMARY.md`
</output>
