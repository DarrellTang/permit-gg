---
phase: 01-foundation-question-bank
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/app-shell.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/bottom-tabs.tsx
  - src/components/layout/theme-toggle.tsx
  - src/components/layout/page-transition.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/onboarding/page.tsx
  - src/hooks/use-media-query.ts
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "App shell renders sidebar on desktop (lg+) and bottom tab bar on mobile"
    - "Navigation includes items for: Dashboard, Practice, Simulated Test, Analytics, Flashcards"
    - "Theme toggle switches between dark and light mode"
    - "Page transitions use Motion with cyberpunk-style animations"
    - "Dashboard placeholder page loads at /dashboard route"
    - "Layout is touch-friendly with adequate tap targets on mobile"
  artifacts:
    - path: "src/components/layout/app-shell.tsx"
      provides: "Responsive shell with sidebar (desktop) and bottom tabs (mobile)"
      exports: ["AppShell"]
    - path: "src/components/layout/sidebar.tsx"
      provides: "Desktop sidebar with nav items, logo, and theme toggle"
      exports: ["Sidebar"]
    - path: "src/components/layout/bottom-tabs.tsx"
      provides: "Mobile bottom tab bar with icon navigation"
      exports: ["BottomTabs"]
    - path: "src/components/layout/theme-toggle.tsx"
      provides: "Dark/light mode toggle button"
      exports: ["ThemeToggle"]
    - path: "src/app/(app)/layout.tsx"
      provides: "App route group layout wrapping children in AppShell"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard placeholder with cyberpunk styling"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/layout/app-shell.tsx"
      via: "Layout renders AppShell wrapping children"
      pattern: "AppShell.*children"
    - from: "src/components/layout/app-shell.tsx"
      to: "src/components/layout/sidebar.tsx"
      via: "Renders Sidebar on lg+ breakpoint"
      pattern: "hidden lg:flex.*Sidebar"
    - from: "src/components/layout/app-shell.tsx"
      to: "src/components/layout/bottom-tabs.tsx"
      via: "Renders BottomTabs on mobile"
      pattern: "lg:hidden.*BottomTabs"
    - from: "src/components/layout/sidebar.tsx"
      to: "src/components/layout/theme-toggle.tsx"
      via: "Sidebar includes theme toggle at bottom"
      pattern: "ThemeToggle"
---

<objective>
Build the responsive app shell with adaptive navigation (sidebar on desktop, bottom tabs on mobile), cyberpunk-styled dashboard placeholder, onboarding flow placeholder, and page transitions.

Purpose: The app shell is the structural container for all app features. Getting navigation, responsiveness, and theming right here means every future page just slots in.
Output: A navigable app shell at /dashboard with sidebar/tabs, theme toggle, and placeholder content styled in the Magical Girl Cyberpunk aesthetic.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-question-bank/01-CONTEXT.md
@.planning/phases/01-foundation-question-bank/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 - theming and fonts the executor needs -->
From src/app/globals.css (expected after Plan 01):
- CSS variables: --primary (hot pink), --secondary (purple), --accent (cyan), --neon-pink, --neon-purple, --neon-cyan
- Tailwind theme tokens: bg-primary, text-primary, bg-card, border-border, etc.
- Font families: font-display (Orbitron), font-ui (Chakra Petch), font-body (Inter)
- Utility classes: .neon-glow, .neon-text

From src/components/theme-provider.tsx:
```typescript
// "use client" wrapper around next-themes ThemeProvider
// Already in root layout -- no need to add again
```

From src/lib/constants/categories.ts:
```typescript
export const CATEGORIES: Array<{
  slug: string; name: string; description: string;
  icon: string; weight: number; sortOrder: number;
}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build adaptive navigation components (sidebar + bottom tabs + theme toggle)</name>
  <files>src/components/layout/app-shell.tsx, src/components/layout/sidebar.tsx, src/components/layout/bottom-tabs.tsx, src/components/layout/theme-toggle.tsx, src/components/layout/page-transition.tsx, src/hooks/use-media-query.ts</files>
  <action>
    All components are "use client" (they use hooks and interactivity).

    1. Create `src/hooks/use-media-query.ts` -- Custom hook that returns boolean for a media query match. Use `window.matchMedia` with SSR safety (default false on server). Used for conditional rendering logic if needed beyond Tailwind classes.

    2. Create `src/components/layout/theme-toggle.tsx`:
       - Uses `useTheme()` from `next-themes`
       - Renders a button (shadcn Toggle or custom) that cycles between dark/light
       - Icon: sun for light, moon for dark (use simple SVG or emoji initially)
       - Styled with cyberpunk accent colors -- neon glow on hover
       - Mounted state check to avoid hydration mismatch (common next-themes pattern)

    3. Create `src/components/layout/sidebar.tsx`:
       - Desktop sidebar (width ~64/256px, collapsible later)
       - Top section: "PERMIT.GG" logo text in Orbitron font (font-display) with neon-pink text color
       - Nav items with icons and labels: Dashboard (home icon), Practice (play icon), Simulated Test (clipboard icon), Analytics (chart icon), Flashcards (cards icon)
       - Use `next/link` with `usePathname()` for active state highlighting
       - Active nav item has neon-pink left border + subtle bg highlight
       - Bottom section: ThemeToggle component + settings icon
       - Cyberpunk styling: dark card background, subtle border-right with purple tint, hover states with glow effect
       - Nav items for Practice, Simulated Test, Analytics, Flashcards should link to placeholder routes (they won't exist yet -- that's fine, nav is structural)

    4. Create `src/components/layout/bottom-tabs.tsx`:
       - Fixed bottom nav bar for mobile
       - 5 tab items matching sidebar: Dashboard, Practice, Test, Analytics, Cards
       - Icons above short labels
       - Active tab highlighted with neon-pink color
       - Touch-friendly: minimum 44px tap targets (per WCAG)
       - Background: card color with top border
       - Safe area padding for notched phones: `pb-safe` or manual padding

    5. Create `src/components/layout/app-shell.tsx`:
       - Composes Sidebar (hidden on mobile, flex on lg+) and BottomTabs (visible on mobile, hidden on lg+)
       - Main content area with `overflow-y-auto` and `pb-20 lg:pb-0` (space for bottom tabs on mobile)
       - Uses Tailwind responsive classes only (no JS media query needed for show/hide)
       - Wraps children in PageTransition for animated route changes

    6. Create `src/components/layout/page-transition.tsx`:
       - Uses Motion `AnimatePresence` with `mode="wait"`
       - `usePathname()` as key for route change detection
       - Cyberpunk transition: slight horizontal slide + blur filter (glitch feel)
       - Duration ~200ms, easeInOut
       - Exact pattern from RESEARCH.md code example
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AppShell component renders sidebar on desktop and bottom tabs on mobile. Sidebar has PERMIT.GG branding, 5 nav items, and theme toggle. Bottom tabs have 5 icon+label items with 44px touch targets. Page transitions animate between routes. All components use cyberpunk theming.</done>
</task>

<task type="auto">
  <name>Task 2: Create app route group with dashboard and onboarding pages</name>
  <files>src/app/(app)/layout.tsx, src/app/(app)/dashboard/page.tsx, src/app/(app)/onboarding/page.tsx</files>
  <action>
    1. Create `src/app/(app)/layout.tsx`:
       - Import AppShell from `@/components/layout/app-shell`
       - Render: `<AppShell>{children}</AppShell>`
       - This is a nested layout under root layout (fonts + ThemeProvider already applied)
       - No additional providers needed

    2. Create `src/app/(app)/dashboard/page.tsx` (Server Component):
       - Title: "Welcome to PERMIT.GG" in Orbitron font (font-display) with neon gradient text
       - Subtitle: "Your CA permit test prep starts here" in Chakra Petch (font-ui)
       - Placeholder cards using shadcn Card component, styled with cyberpunk theme:
         - "Readiness Score" card (shows "--%" placeholder, will be live in Phase 4)
         - "Quick Practice" card (CTA button, will link to quiz in Phase 2)
         - "Simulated Test" card (CTA button, will link to sim test in Phase 2)
         - "Recent Activity" card (shows "No quizzes yet" placeholder)
       - Cards should have subtle neon border glow on hover
       - Use grid layout: 2 columns on desktop, 1 column on mobile (Tailwind `grid grid-cols-1 lg:grid-cols-2 gap-4`)
       - The cards are structural placeholders -- they show the dashboard vision but buttons don't navigate to functional pages yet

    3. Create `src/app/(app)/onboarding/page.tsx`:
       - 3-4 step walkthrough placeholder
       - Step 1: "AI-Powered Questions" -- questions change every session
       - Step 2: "Know Your Weaknesses" -- category analytics show where to focus
       - Step 3: "Pass Your Test" -- readiness score tells you when you're ready
       - Step 4: "Start Studying" -- CTA to dashboard
       - Use Motion for step transitions (slide in from right)
       - Each step: large icon/emoji, heading in Orbitron, description in Inter
       - Progress dots at bottom showing current step
       - Cyberpunk card styling with neon accents
       - This is a placeholder implementation -- content and flow will be refined later

    4. Update or remove `src/app/page.tsx` (the default Next.js page) -- this route will be handled by the marketing layout in Plan 04. For now, redirect to /dashboard: `redirect("/dashboard")` from `next/navigation`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>App route group at (app)/ with layout wrapping children in AppShell. Dashboard page at /dashboard shows cyberpunk-styled placeholder cards. Onboarding page at /onboarding has 3-4 step walkthrough with Motion transitions. Root / redirects to /dashboard. App builds without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` -- navigate to localhost:3000/dashboard and see the app shell
3. Sidebar visible on desktop (lg+), bottom tabs visible on mobile viewport
4. Theme toggle switches between dark and light mode
5. /onboarding shows step-through walkthrough
6. Navigation items are present (even if target pages don't exist yet)
</verification>

<success_criteria>
- AppShell renders sidebar on desktop (lg+ breakpoint) and bottom tabs on mobile
- Sidebar shows "PERMIT.GG" branding in Orbitron font with 5 nav items
- Bottom tabs have 5 items with icons and labels, minimum 44px touch targets
- Theme toggle works (dark <-> light)
- Dashboard at /dashboard shows placeholder cards with cyberpunk styling
- Onboarding at /onboarding has multi-step walkthrough
- Page transitions animate with cyberpunk glitch effect
- All layouts are responsive (no horizontal overflow on mobile)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-question-bank/01-03-SUMMARY.md`
</output>
