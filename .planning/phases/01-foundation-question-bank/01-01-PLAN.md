---
phase: 01-foundation-question-bank
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/fonts/index.ts
  - src/lib/types/question.ts
  - src/lib/constants/categories.ts
  - src/lib/constants/theme.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/components/theme-provider.tsx
  - vitest.config.mts
  - tests/question-schema.test.ts
  - .env.local.example
autonomous: true
requirements:
  - QENG-06
  - UI-02

must_haves:
  truths:
    - "Next.js 16 app builds and starts without errors"
    - "Tailwind 4 CSS-first theming with OKLCH cyberpunk palette is configured"
    - "Dark mode is the default theme with light mode toggle support"
    - "Question type definitions enforce 4-option multiple choice format"
    - "Database connectivity confirmed from server and browser contexts"
    - "Question format validated by automated test suite"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Cyberpunk OKLCH color system with dark-first theming"
      contains: "@theme inline"
    - path: "src/lib/types/question.ts"
      provides: "SeedQuestion type + Zod schema enforcing 4-option format"
      exports: ["SeedQuestionSchema", "SeedQuestion", "CategorySlug", "CATEGORY_WEIGHTS"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory"
      exports: ["createClient"]
    - path: "src/lib/supabase/client.ts"
      provides: "Browser-side Supabase client factory"
      exports: ["createClient"]
    - path: "vitest.config.mts"
      provides: "Vitest configuration for React testing"
    - path: "tests/question-schema.test.ts"
      provides: "Schema validation tests for question format"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/fonts/index.ts"
      via: "CSS variable classes on html element"
      pattern: "className=.*font-orbitron.*font-chakra.*font-inter"
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "ThemeProvider wrapping children"
      pattern: "ThemeProvider.*defaultTheme.*dark"
    - from: "src/app/globals.css"
      to: "shadcn/ui components"
      via: "@theme inline CSS variables"
      pattern: "--color-primary.*var\\(--primary\\)"
---

<objective>
Scaffold the Next.js 16 project with all Phase 1 dependencies, configure the Magical Girl Cyberpunk theming system with Tailwind 4 CSS-first approach, define type contracts and Supabase client factories, and set up Vitest with initial test scaffolds.

Purpose: Every subsequent plan depends on the project scaffold, theming, types, and test infrastructure. This is the foundation layer.
Output: A buildable Next.js app with cyberpunk theming, type definitions, Supabase clients, and a passing test suite.
</objective>

<execution_context>
@/Users/darrelltang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darrelltang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-question-bank/01-CONTEXT.md
@.planning/phases/01-foundation-question-bank/01-RESEARCH.md
@.planning/phases/01-foundation-question-bank/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, .env.local.example, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --turbopack` to scaffold into the current directory (project is greenfield, no existing src/).

    2. Install Phase 1 runtime dependencies:
       `npm install @supabase/supabase-js @supabase/ssr zod next-themes motion`

    3. Install dev dependencies:
       `npm install -D prettier prettier-plugin-tailwindcss vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths`

    4. Initialize shadcn/ui:
       `npx shadcn@latest init` -- accept defaults, it will configure for Tailwind 4 + OKLCH

    5. Add Phase 1 shadcn components:
       `npx shadcn@latest add button card tabs navigation-menu sheet separator badge skeleton toggle`

    6. Create `.env.local.example` with:
       ```
       NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
       ```

    7. Verify the project builds with `npm run build` (or at minimum `npx next build`).

    NOTE: Do NOT customize globals.css or layout.tsx yet -- Task 2 handles theming. Just confirm the scaffold works.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js 16 project scaffolded with all dependencies installed, shadcn/ui initialized, and project builds without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure cyberpunk theming, fonts, types, and Supabase clients</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/fonts/index.ts, src/lib/types/question.ts, src/lib/constants/categories.ts, src/lib/constants/theme.ts, src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/components/theme-provider.tsx, vitest.config.mts, tests/question-schema.test.ts</files>
  <behavior>
    - Test: SeedQuestionSchema validates a well-formed question object (all fields present, 3 wrong answers)
    - Test: SeedQuestionSchema rejects question with only 2 wrong answers
    - Test: SeedQuestionSchema rejects question with empty question_text
    - Test: CategorySlug accepts all 8 valid category slugs
    - Test: CategorySlug rejects an invalid slug like "invalid-category"
    - Test: CATEGORY_WEIGHTS values sum to 1.0
    - Test: MIN_QUESTIONS_PER_CATEGORY is 25 and TARGET_TOTAL_QUESTIONS is 300
  </behavior>
  <action>
    1. Create `src/fonts/index.ts` -- Configure Orbitron (display), Chakra Petch (UI), and Inter (body) via `next/font/google` with CSS variable output (`--font-orbitron`, `--font-chakra`, `--font-inter`). Use `display: "swap"` for all three. Chakra Petch needs explicit weights: 400, 500, 600, 700.

    2. Replace `src/app/globals.css` with the full cyberpunk theme:
       - Import tailwindcss
       - `@custom-variant dark (&:where(.dark, .dark *));`
       - `@theme inline` block mapping semantic colors to CSS variables AND mapping font families (`--font-display: var(--font-orbitron)`, `--font-ui: var(--font-chakra)`, `--font-body: var(--font-inter)`)
       - `.dark` block (PRIMARY theme): deep chrome background, hot pink primary, electric purple secondary, cyan accent, neon glow colors -- use exact OKLCH values from RESEARCH.md
       - `:root` block (light mode alternate): inverted for readability -- use exact OKLCH values from RESEARCH.md
       - Add utility classes: `.neon-glow` (box-shadow using neon-pink), `.neon-text` (text-shadow using neon colors)

    3. Create `src/components/theme-provider.tsx` -- Thin wrapper around next-themes `ThemeProvider` with `"use client"` directive. Props: `attribute="class"`, `defaultTheme="dark"`, `enableSystem`, `disableTransitionOnChange`.

    4. Update `src/app/layout.tsx`:
       - Import fonts from `@/fonts`
       - Apply font CSS variable classes to `<html>` element
       - Add `suppressHydrationWarning` to `<html>`
       - Wrap children in `ThemeProvider` with `defaultTheme="dark"`
       - Set `<body className="font-body antialiased">`
       - Add metadata: title "PERMIT.GG", description about CA DMV permit test prep

    5. Create `src/lib/types/question.ts` -- Define Zod schemas and TypeScript types exactly as shown in RESEARCH.md code examples: `CategorySlug` enum (8 slugs), `SeedQuestionSchema` (id, category_id, question_text, correct_answer, wrong_answers array length 3, explanation, handbook_reference, source), exported types, `CATEGORY_WEIGHTS` record, `MIN_QUESTIONS_PER_CATEGORY = 25`, `TARGET_TOTAL_QUESTIONS = 300`.

    6. Create `src/lib/constants/categories.ts` -- Export `CATEGORIES` array with objects for each of the 8 categories: `{ slug, name, description, icon (emoji), weight, sortOrder }`. Use exact weights from RESEARCH.md (road-signs: 0.15, right-of-way: 0.15, traffic-laws: 0.15, speed-limits: 0.10, dui-drug-laws: 0.10, safe-driving: 0.15, parking: 0.10, sharing-the-road: 0.10).

    7. Create `src/lib/constants/theme.ts` -- Export animation configuration constants for the cyberpunk theme: transition durations, spring configs for Motion animations, glow shadow values. This is a reference file for consistent animation behavior across components.

    8. Create `src/lib/supabase/server.ts` -- Server-side Supabase client factory using `@supabase/ssr` `createServerClient` with cookie handling via `next/headers`. Exact pattern from RESEARCH.md.

    9. Create `src/lib/supabase/client.ts` -- Browser-side Supabase client factory using `@supabase/supabase-js` `createClient` with env vars `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

    10. Create `vitest.config.mts` -- Configure Vitest with `@vitejs/plugin-react`, jsdom environment, `vite-tsconfig-paths` for path aliases, and test include pattern `tests/**/*.test.ts`.

    11. Create `tests/question-schema.test.ts` -- Write tests per the behavior block above. Import from `@/lib/types/question`.

    12. Run tests RED first, then verify they pass GREEN after implementation.
  </action>
  <verify>
    <automated>npx vitest run tests/question-schema.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <done>Cyberpunk OKLCH theming configured in globals.css with dark-first approach. Three fonts loaded via next/font. ThemeProvider wraps app with dark default. Question types with Zod validation enforce 4-option format. Supabase client factories exist for server and browser. Vitest passes all question schema tests. App builds without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run --reporter=verbose` -- all tests pass
3. `npm run dev` starts and loads at localhost:3000 without errors
4. Dark theme is applied by default (dark background visible)
</verification>

<success_criteria>
- Next.js 16 project scaffolded and builds cleanly
- All Phase 1 dependencies installed (Supabase, Motion, next-themes, Zod, shadcn/ui)
- Cyberpunk OKLCH color palette defined in globals.css with dark-first approach
- Orbitron, Chakra Petch, Inter fonts configured via next/font/google
- SeedQuestion Zod schema enforces 4-option multiple choice format
- Supabase server and browser client factories created
- Vitest configured and question schema tests pass
- ThemeProvider defaults to dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-question-bank/01-01-SUMMARY.md`
</output>
